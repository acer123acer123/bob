For migration.....
school_familymember.username = varchar(255)
Images and uploaded PDF files in course catalog don't point to right folder (DB change)
Mailgun - remove footer
Mailgun - add database column


Family Profile 
  - New family form - must use unique email address.
  - If userid is not available (new or modify) then don't let the user submit
  - If email is not available (new) then don't let the user submit
